# Database Configuration
DB_USER=postgres
DB_PASSWORD=password
DB_NAME=zpmeow
DB_HOST=localhost
DB_PORT=5432
DB_SSLMODE=disable

# Server Configuration
GIN_MODE=debug
SERVER_PORT=8080

# Logger Configuration
LOG_LEVEL=info                    # debug, info, warn, error, fatal
LOG_FORMAT=console                # console, json
LOG_CONSOLE_COLOR=true            # Enable colors in console output
LOG_FILE_ENABLED=true             # Enable file logging
LOG_FILE_PATH=log/app.log         # Path to log file
LOG_FILE_MAX_SIZE=100             # Maximum size in MB before rotation
LOG_FILE_MAX_BACKUPS=3            # Number of backup files to keep
LOG_FILE_MAX_AGE=28               # Maximum age in days to keep log files
LOG_FILE_COMPRESS=true            # Compress rotated log files
LOG_FILE_FORMAT=json              # Format for file logs: console, json
